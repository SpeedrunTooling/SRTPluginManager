<UserControl
    x:Class="SRTPluginManager.MVVM.View.UpdatesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="640"
    d:DesignWidth="1024">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Fonts/#Teko" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style TargetType="Button" x:Key="HoverButtonStyle">
            <Setter Property="Background" Value="#151527" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Hover Trigger -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A4A" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <!-- Header -->
        <StackPanel Height="60" Orientation="Horizontal">
            <Image Margin="10" Source="/Images/Plugins.png" />
            <TextBlock
                Margin="10,10,0,10"
                HorizontalAlignment="Left"
                FontSize="28"
                FontWeight="Bold"
                Foreground="White"
                Text="Updates" />
        </StackPanel>

        <!-- Main Content -->
        <StackPanel Orientation="Vertical" Margin="10">
            <!-- Info Section -->
            <Border Background="#151527" Padding="15" CornerRadius="10" Margin="0,0,0,20">
                <TextBlock FontSize="18" TextWrapping="Wrap" Text="Keep your plugins up-to-date to ensure optimal performance and compatibility." />
            </Border>

            <!-- Check for Updates Button -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button
                    Width="150"
                    Height="50"
                    Content="Check for Updates"
                    Cursor="Hand"
                    Style="{StaticResource HoverButtonStyle}"
                    Click="CheckForUpdates_Click" />
            </StackPanel>

            <!-- Updates Section -->
            <Grid Margin="0,0,0,20" Height="357">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <Border Grid.Column="1" Background="#151527" Padding="15" CornerRadius="10" Margin="10,10,10,81">
                    <StackPanel>
                        <TextBlock FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" ><Run Text="Release"/><Run Language="de-de" Text=" "/><Run Text="1.0.1"/><Run Text=".0"/></TextBlock>
                        <TextBlock FontSize="20" Text="Released on: 2024-12-23" Margin="0,10,0,0" />
                        <TextBlock FontSize="20" Text="Changelog:" FontWeight="Bold" Margin="0,10,0,5" />
                        <StackPanel Margin="10,0,0,0">
                            <TextBlock FontSize="16" Text="• Reworked the design for Plugins" />
                            <TextBlock FontSize="16" Text="• Added a Contact us Tab." />
                            <TextBlock FontSize="16" Text="• Adjusted the Home Tab." />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="#151527" Padding="15" CornerRadius="10" Margin="10,10,10,81">
                    <StackPanel>
                        <TextBlock FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" ><Run Text="Release "/><Run Text="1.0."/><Run Language="de-de" Text="1.1"/></TextBlock>
                        <TextBlock FontSize="20" Text="Released on: 2025-05-03" Margin="0,10,0,0" />
                        <TextBlock FontSize="20" Text="Changelog:" FontWeight="Bold" Margin="0,10,0,5" />
                        <StackPanel Margin="10,0,0,0">
                            <TextBlock FontSize="16" Text="• Added a new Updates Tab." />

                            <TextBlock FontSize="16" Text="• Updated Contact us Tab." />
                            <TextBlock FontSize="16" Text="• Formatted Design." />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </StackPanel>
</UserControl>
